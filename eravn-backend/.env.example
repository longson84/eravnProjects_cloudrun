# ==========================================
# eravnProjects Backend - Environment Variables
# ==========================================

# Server
PORT=8080

# Google Cloud Project ID (for Firestore)
GCP_PROJECT_ID=eravn-projects-cloud-run

# Security: Token for Cloud Scheduler to call /api/sync/all
CRON_SECRET=your-random-secret-token

# Google Chat Webhook URL (optional)
WEBHOOK_URL=

# Sync Configuration
SYNC_CUTOFF_SECONDS=300
MAX_RETRIES=3
BATCH_SIZE=450

# CORS: Frontend URL (comma-separated for multiple origins)
CORS_ORIGINS=http://localhost:5173

# OAuth2: Google Drive Authentication
# Get these from Google Cloud Console > APIs & Services > Credentials > OAuth 2.0 Client IDs
GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-client-secret
GOOGLE_REFRESH_TOKEN=your-refresh-token
